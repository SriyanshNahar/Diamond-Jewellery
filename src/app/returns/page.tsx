import React from 'react';

export default function ReturnsPage() {
  return (
    <div style={{ padding: '8rem 2rem 5rem', minHeight: '100vh', background: 'var(--color-white)' }}>
      <div className="container" style={{ maxWidth: '800px', margin: '0 auto' }}>
        <h1 style={{ fontSize: '2.5rem', marginBottom: '2rem', color: 'var(--color-black)' }}>Return & Exchange</h1>
        <p style={{ color: 'var(--color-gray)', lineHeight: 1.8 }}>We offer a 100% lifetime exchange policy and a hassle-free 14-day return window for all standard purchases.</p>
      </div>
    </div>
  );
}
